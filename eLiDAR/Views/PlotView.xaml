<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="eLiDAR.Views.PlotView"
             Title="Plots">
  
  <ListView x:Name="MyListView"
            ItemsSource="{Binding FakePlots}"
            ItemTapped="Handle_ItemTapped"
            CachingStrategy="RecycleElement"
            RowHeight="100 ">
    
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
             </Grid.ColumnDefinitions>
             <Button Text=" Edit" Grid.Row = "1" Grid.Column=" 2" Grid.RowSpan=" 2" Pressed="Edit_Plot"/>
             <Button Text=" Trees" Grid.Row="1" Grid.Column="3" Grid.RowSpan=" 2" Pressed= "View_Trees"/>
             <Label Text="{Binding PlotNum, StringFormat='Plot : {0:0.}'}" Grid.Row=" 1" Grid.Column=" 1" FontSize="Medium" FontAttributes="Bold"/>
             <Label Text="{Binding PlotType, StringFormat='Plot Type : {0:N}'}" Grid.Row=" 2" Grid.Column="1" FontSize="Subtitle" />
          </Grid>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
    
  </ListView>
</ContentPage>
