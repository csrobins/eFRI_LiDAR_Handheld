<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" Title="Add Soil Layer" BackgroundColor="#D5E7FA" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
 x:Class="eLiDAR.Views.AddSoil" xmlns:local="clr-namespace:eLiDAR.Views" BindingContext="AddSoilViewModel">
    <ScrollView>
        <StackLayout Padding="20" Spacing="12">
            <local:SoilView/>
            <Button x:Name="SubmitButton" Command="{Binding AddCommand}" BorderRadius="0" Text="Save Soil" FontAttributes="Bold" TextColor="White" BackgroundColor="#5989B5"/>
            <Label Text="View All Soils" IsVisible="{Binding IsViewAll}" x:Name="ViewLbl" TextColor="Black" HorizontalOptions="EndAndExpand" FontSize="15">  
                <Label.GestureRecognizers>  
                <TapGestureRecognizer Command="{Binding ViewAllCommand}" NumberOfTapsRequired="1" />  
                </Label.GestureRecognizers>  
            </Label>  
    </StackLayout>
    </ScrollView>
</ContentPage>