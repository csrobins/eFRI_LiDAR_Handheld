<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="eLiDAR.Views.AddProject"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:eLiDAR.Views"
    Title="Add Project"
    BackgroundColor="{DynamicResource backgroundColor}"
    BindingContext="AddProjectViewModel"
    Shell.BackgroundColor="{DynamicResource backgroundColor}"
    Shell.ForegroundColor="{StaticResource cerulean}"
    Shell.TitleColor="{StaticResource cerulean}">

    <StackLayout Padding="20" Spacing="12">
        <local:ProjectView />

     <!--   <Button
            x:Name="SubmitButton"
            BackgroundColor="{StaticResource cerulean}"
            BorderRadius="0"
            Command="{Binding AddProjectCommand}"
            FontAttributes="Bold"
            Text="Save Project"
            TextColor="White" /> -->
        <!--<Label
            x:Name="ViewLbl"
            FontSize="15"
            HorizontalOptions="EndAndExpand"
            IsVisible="{Binding IsViewAll}"
            Text="View All Projects"
            TextColor="Black">
            <Label.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding ViewAllProjectsCommand}" NumberOfTapsRequired="1" />
            </Label.GestureRecognizers>
        </Label>-->
    </StackLayout>
</ContentPage>