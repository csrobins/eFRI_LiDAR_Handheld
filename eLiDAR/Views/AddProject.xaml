<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" Title="Add Project" BackgroundColor="#D5E7FA" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
 x:Class="eLiDAR.Views.AddProject" xmlns:local="clr-namespace:eLiDAR.Views" BindingContext="AddProjectViewModel">
    <StackLayout Padding="20" Spacing="12">
        <local:ProjectView />

        <Button x:Name="SubmitButton" Command="{Binding AddProjectCommand}" BorderRadius="0" Text="Save Project" FontAttributes="Bold" TextColor="White" BackgroundColor="#5989B5"/>
        <Label Text="View All Projects" IsVisible="{Binding IsViewAll}" x:Name="ViewLbl" TextColor="Black" HorizontalOptions="EndAndExpand" FontSize="15">  
        <Label.GestureRecognizers>  
        <TapGestureRecognizer Command="{Binding ViewAllProjectsCommand}" NumberOfTapsRequired="1" />  
        </Label.GestureRecognizers>  
        </Label>  
    </StackLayout>
</ContentPage>